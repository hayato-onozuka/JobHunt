<li>  
  <%= image_for(note.user) %>
  <span class="content">
   タイトル： <%= link_to note.title, note_path(note.id) %>
  </span>
  <span>
    <%= "投稿日：" + note.created_at.strftime("%Y-%m-%d") %>
  </span>
</li>
